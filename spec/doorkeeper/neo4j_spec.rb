require 'spec_helper'

describe Doorkeeper::Neo4j do
  it 'has a version number' do
    expect(Doorkeeper::Neo4j::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
